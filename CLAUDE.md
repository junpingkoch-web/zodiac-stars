# Project: zodiac-stars（生肖与星座计算器）

零构建静态站，单文件结构：全部逻辑内联在一个 ~114KB 的 `index.html` 里（比 watch-valuator 更大，
同样是单文件套路，不要主动拆分成多文件）。中国生肖 + 西方星座计算器，标题走德语
"Chinesisches Tierkreiszeichen & Sternzeichen Rechner"。

## 文件结构
- `index.html` — 唯一核心文件
- `images/` — 配图
- `ads.txt` / `robots.txt` / `sitemap.xml` / `README.md` — 已补齐，跟其他仓库对齐

## 符合的生态约定
- `data-i18n` + `localStorage` 的手搓 i18n 模式，跟其他 sibling 工具一致
- AdSense (`pub-4830421367394194`) 和 GA4 (`G-7LZZQ43QD6`) 都已正确接入

## Commands
- 无构建/测试命令
- 本地预览：共享配置在 `C:\Users\junpi\.claude\.claude\launch.json`（先确认这个仓库是否已经在里面注册了端口，没有的话要新增一条）

## 部署流程
- 改完直接 commit + push 到 `main`
- Commit 作者身份：`Junping Koch <junping.koch@gmail.com>`，仓库单独设置

## 明确禁止的事
- 不要把单文件拆成 `index.html`/`style.css`/`script.js` 三件套，除非被明确要求——这是这个工具家族里单文件类工具的既定选择

## UI 与无障碍规范
2026-08-29 用 `ui-reviewer`/`qa-tester` 实测发现并修复过的问题，以后改动这个文件时保持这些约束：
- **触控区域**：可点击元素（语言切换按钮、打赏按钮、页脚链接）保持 ≥44×44px 的实际响应区域（用浏览器 `getBoundingClientRect()` 实测确认，不要只看 CSS 数值估算）。
- **SVG 移动端缩放**：生肖轮这类内嵌固定 viewBox 坐标的 SVG，`width:100%` 整体缩放会让内部文字跟着等比缩小到不可读。**不要用视口宽度断点**（如 `@media max-width:640px`）来判断要不要限制最小宽度——轮子实际渲染宽度还要再减去 `.shell`/`.tool-card` 的内边距，视口断点和容器实际宽度对不上，会在中间尺寸区间制造"比两侧都更差"的回归（2026-08-29 踩过一次）。正确做法：`width:100%` 配合无条件的 `min-width:760px`，让容器不够宽时自然触发横向滚动，不用猜视口阈值。给 SVG 内文字加 CSS `font-size` 覆盖没用，会被 viewBox 的整体缩放一起吃掉——字号问题只能改 `buildWheel()` 里生成 `<text font-size="...">` 的数值本身。当前年份文字用的是 22（原为 18）、中心"1900-2026"用的是 14（原为 11）——这两个数值是在截图确认不会造成相邻文字视觉重叠的前提下选的，不代表已经达到 12px 的理想可读性下限。**已经实测过 27/19 这组更大的数值：会导致"狗/鸡/猴"等好几个生肖格子里的年份数字明显重叠、糊成一团（截图实锤，2026-08-29），不要再建议或尝试这个区间——22/14 目前是验证过的安全上限**，再调大之前必须先截图确认最内圈（`yearRadii` 最小的那一环，径向+角向间距最紧）没有重叠，不能只凭理论换算就调大。
- **裁切区域要有滚动提示**：轮子横向滚动时，用 `mask-image` 加渐隐边缘 + 初始 `scrollLeft` 居中（而不是让浏览器默认停在最左边）——否则看起来像是页面渲染坏了，不是"可以滚动"。
- **测量刚渲染内容的尺寸要用双重 `requestAnimationFrame`**：类似"设置完 SVG/DOM 内容后立刻读 `scrollWidth`/`clientWidth` 来定位"这种逻辑，单次 `requestAnimationFrame` 在首次加载时可能在布局真正落定前就触发（2026-08-29 实测踩到：单次 rAF 时 `scrollLeft` 一直读到 0，改成 `requestAnimationFrame(()=>requestAnimationFrame(fn))` 嵌套两层才稳定生效）。
- **无障碍状态**：有开关/选中态的按钮（如语言切换）要在状态变化时同步设置 `aria-pressed`，不能只切换 `.active` 这个视觉类。
- **验证 SVG 极坐标文字有没有视觉重叠，截图肉眼看比写脚本比较包围盒可靠**：轮子里的文字是按角度+半径排列的（12 个生肖 × 6 环年份），"比较 DOM 顺序相邻的两个 `<text>` 元素包围盒"这种脚本容易把不同生肖的文字错误配对在一起（角度差 30 度，视觉上根本不相邻），产出一堆假阳性。改字号后要确认没重叠，直接截图看最拥挤的那一圈（最内环，弧长最短）更快更准。
- **多视角复审**：这个仓库只有 `index.html` 一个核心文件、~114KB，改动这类核心文件时单次 `ui-reviewer` 审查不算穷尽——值得的话可以派生两个独立的 `ui-reviewer` 交叉验证（用全局的 `~/.claude/agents/ui-reviewer.md`，不需要项目本地副本）。2026-08-29 这次实测中，双视角审查真的抓到了当天早些时候自己修复引入的新回归（断点区间问题、裁切无提示问题），不是走过场。
- **没有测试基建**：`qa-tester` 会如实汇报这个仓库没有 `package.json`/测试框架，这是正确行为，不是它没跑起来。

## 持续维护
每次你需要重复纠正 Claude 同一件事三次以上，就把结论补进这个文件对应章节。
